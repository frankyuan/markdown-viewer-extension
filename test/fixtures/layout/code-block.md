# Code Block Fixture

```javascript
const answer = 42;
function hello(name) {
  return `Hello, ${name}!`;
}
```

Inline `code` inside a paragraph.

```markdown
# Heading in a fence

- [x] task with **strong**
```

